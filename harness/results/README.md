# results/ —— 跑批产出

评测 / 调优 / 调用的产出落这里（plan §4 截图清单的素材库）：

- `screenshots/` —— 开发过程截图，按节点编号命名（① 基础部署 … ⑧ 多 agent 协作，见 `harness/plan.md` §4）
- `traces/` —— P0 起的 QwenPaw 调用 trace（`traces.jsonl`，结构见 `backend/app/observability/trace.py`）
- 分数表 / before-after 对比 / 分数曲线图（P2 跑批产出）

> `screenshots/` 与 `traces/` 已建（含 `.gitkeep`）。`*.jsonl`、大图等可在 `.gitignore` 视情况忽略。
