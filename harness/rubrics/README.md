# rubrics/ —— 打分标准

存放评测打分的两类标准（plan P2）：

- **规则项**（零成本、可重复）：引来源 / 在范围内 / 事实正确 / JSON schema 合法 等
- **LLM-judge prompt**：便宜模型当裁判的 prompt 模板，测试集 ≤20 条

> 当前为空。P2 落 `rule_checks.py` + `llm_judge_prompt.md`。
